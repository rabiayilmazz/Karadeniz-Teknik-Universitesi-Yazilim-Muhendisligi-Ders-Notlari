for i in 1 2 3 4 5
do
  mkdir -p backup/ch0${i}
  if [ $? -eq 0 ] ; then
    for j in doc c h m pl sh
    do
      cp ch0${i}/*.${j} backup/ch0${i}
      if [ $? -ne 0 ] ; then break 2 ; fi
    done
  else
    echo "Could not make backup directory."
  fi
done

