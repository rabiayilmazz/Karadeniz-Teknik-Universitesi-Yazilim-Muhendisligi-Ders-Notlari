x=0 
while [ "$x" -lt 10 ] ; # this is loop1 
do 
    y="$x"
    while [ "$y" -ge 0 ] ; # this is loop2 
    do 
        echo "$y \c" 
        y=`echo "$y - 1" | bc`
    done 
    echo 
    x=`echo "$x + 1" | bc`
done
